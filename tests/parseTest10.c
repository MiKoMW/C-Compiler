struct emm{
int a;
};
char in[2];
char compare[4];

int main() {


    memset(in, 0, sizeof(int));
        memset(in, 0, sizeof(struct emm));

    //memset(compare, 0, sizeof(compare)); can only be type.
    while (scanf("%s%s", in, compare)) {

            if(compare[con] == in[idx]){
            }

        if(flag){
            printf("Yes\n");
        }else{
            printf("No\n");
        }

    }
    return 0;
}