hihihihihihiLo

Token class 中 assert 没啥用。

我们支持取址 指针访问吗？

Comment renturn Token

gcc 准许 'aab' = b
gcc error /* /

my lexer don't have &

c语言 短路 or| 优先级

In both cases (global or local), it is illegal to declare twice the same identifiers in the same current block (note that this means it is illegal to declare a variable with the same name as a procedure at the global level).

Op 需要是一个node吗？

final 有啥用？为了保险？

ArrayType 没样例啊。

为啥要return null啊？ 直接 void多好？

visitArrayAccessExpr 没有样例

visitFieldAccessExpr 没有样例

expect return null Token

StrLateral 不见了？

Array 怎么处理啊？

 "-" exp 这鸡儿算啥？


// 用链接会var declear 吗？
public Void visitArrayAccessExpr(ArrayAccessExpr arrayAccessExpr){

structacces如何检查.





如果 main 没有 return 怎么办？？？？？

main函数 带param怎么办啊？

function 给的不对啊！！

dynimic 的 struct怎么办啊？！！！！！！！！！！！搞笑？？？？？？

记得char改成4；

一个register可以装下的 传值， 其他的传址。

我们所有stackoffset记正数，所以到时候是反的，得减去。

stack 和 static index 是反的啊？

return




mcmalloc 需要再确定下。

pointer Type需要思考下。